# nativewind's react-native-css-interop declares a strict peer on react-native-reanimated
# that we don't actually use (only static styling, no animation utilities) and that hasn't
# caught up to RN 0.87 yet. Without this, `npm install` fails with ERESOLVE.
legacy-peer-deps=true
